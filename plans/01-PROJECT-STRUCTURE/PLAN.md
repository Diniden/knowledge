# 01 — PROJECT STRUCTURE PLAN

> **Purpose**: Define every aspect of the monorepo layout, workspace
> configuration, build system, development workflow, and tooling. This plan is
> the foundation — every other plan depends on it.
>
> **Phase**: 1 (Foundation)
> **Dependencies**: None (root plan)
> **Estimated tasks**: 120+

---

## Table of Contents

1. [Monorepo Root Layout](#1-monorepo-root-layout)
2. [Bun Workspace Configuration](#2-bun-workspace-configuration)
3. [Shared Types Package](#3-shared-types-package)
4. [TypeScript Configuration](#4-typescript-configuration)
5. [Vite Configuration](#5-vite-configuration)
6. [ESLint Configuration](#6-eslint-configuration)
7. [Prettier Configuration](#7-prettier-configuration)
8. [SCSS / Styling Configuration](#8-scss--styling-configuration)
9. [Environment Configuration](#9-environment-configuration)
10. [Git Configuration](#10-git-configuration)
11. [Docker Setup](#11-docker-setup)
12. [Build System](#12-build-system)
13. [Development Workflow](#13-development-workflow)
14. [CI/CD Pipeline Configuration](#14-cicd-pipeline-configuration)
15. [Editor Configuration](#15-editor-configuration)
16. [Documentation Structure](#16-documentation-structure)
17. [Script Utilities](#17-script-utilities)

---

## 1. Monorepo Root Layout

Define the complete directory tree with purpose annotations for every directory.

### 1.1 Root Directory Files

- [ ] **PS-RL-001**: Create `package.json` at the repo root
  - Set `"name"` to the project name
  - Set `"private": true` to prevent accidental publishing
  - Set `"type": "module"` for ESM throughout
  - Define `"workspaces"` array for bun workspace discovery
  - Add root-level scripts: `dev`, `build`, `test`, `lint`, `format`, `clean`
- [ ] **PS-RL-002**: Create `bun.lock` (generated by initial `bun install`)
- [ ] **PS-RL-003**: Create `README.md` with project overview and quickstart
  - Include prerequisites (bun version, docker, git)
  - Include setup instructions (`bun install`, `docker compose up`, `bun dev`)
  - Include architecture overview with link to plans/
  - Include contributing guidelines summary
- [ ] **PS-RL-004**: Create `LICENSE` file with chosen license
- [ ] **PS-RL-005**: Create `CONTRIBUTING.md` with development standards
  - Branching strategy
  - Commit message conventions
  - PR review process
  - Code style references

### 1.2 Client Directory (`client/`)

- [ ] **PS-RL-006**: Create `client/` directory as the frontend workspace root
- [ ] **PS-RL-007**: Create `client/package.json`
  - Set `"name"` to `@kg/client`
  - Set `"type": "module"`
  - Define scripts: `dev`, `build`, `preview`, `test`, `lint`
  - Add dependency on `@kg/shared`
  - Add React, ReactDOM as dependencies
  - Add Vite, `@vitejs/plugin-react` as devDependencies
  - Add TypeScript, SCSS as devDependencies
- [ ] **PS-RL-008**: Create `client/ui/` directory for the main platform UI
- [ ] **PS-RL-009**: Create `client/ui/src/` directory for source files
- [ ] **PS-RL-010**: Create `client/ui/src/main.tsx` as the React entry point
- [ ] **PS-RL-011**: Create `client/ui/src/App.tsx` as the root component
- [ ] **PS-RL-012**: Create `client/ui/src/components/` for React components
- [ ] **PS-RL-013**: Create `client/ui/src/hooks/` for custom React hooks
- [ ] **PS-RL-014**: Create `client/ui/src/services/` for API service modules
- [ ] **PS-RL-015**: Create `client/ui/src/store/` for state management
- [ ] **PS-RL-016**: Create `client/ui/src/types/` for frontend-only type definitions
- [ ] **PS-RL-017**: Create `client/ui/src/utils/` for utility functions
- [ ] **PS-RL-018**: Create `client/ui/src/styles/` for global SCSS and theme definitions
- [ ] **PS-RL-019**: Create `client/ui/src/assets/` for static assets (icons, images)
- [ ] **PS-RL-020**: Create `client/ui/src/routes/` for route definitions
- [ ] **PS-RL-021**: Create `client/ui/public/` for static public files
- [ ] **PS-RL-022**: Create `client/ui/index.html` as the Vite HTML entry
- [ ] **PS-RL-023**: Create `client/gen/` directory for generative UI projects
- [ ] **PS-RL-024**: Create `client/gen/.gitkeep` to preserve the directory
- [ ] **PS-RL-025**: Create `client/gen/README.md` explaining the gen UI structure
  - Document the `{user}/{project}/` directory convention
  - Explain how gen UIs are loaded via iframe + ESM
  - Note that contents are git-tracked but agent-generated

#### Design Decisions

> **Q**: Is `client/` a single workspace package, or should `client/ui/` and `client/gen/` be separate packages? The gen UI projects have very different lifecycles from the main UI.
> **A**: `client/` is a single workspace package. The `client/ui/` directory is the main app source, and `client/gen/` is a directory of agent-generated projects. Gen UI projects have their own package.json files but are NOT workspace members — they're independent mini-projects. The workspace package is `@kg/client`, which only covers the main UI.

> **Q**: Should generative UI projects be workspace packages at all, or just unmanaged directories with their own `package.json` files that aren't part of the workspace?
> **A**: Unmanaged directories. Gen UI projects are agent-created, have their own dependencies, and have completely different build lifecycles. Making them workspace packages would mean every `bun install` at the root processes them, and every new agent-generated project modifies the workspace config. They're standalone Vite projects that happen to live inside the repo.

### 1.3 Server Directory (`server/`)

- [ ] **PS-RL-026**: Create `server/` directory as the backend workspace root
- [ ] **PS-RL-027**: Create `server/package.json`
  - Set `"name"` to `@kg/server`
  - Set `"type": "module"`
  - Define scripts: `dev`, `build`, `start`, `test`, `lint`, `migrate`
  - Add dependency on `@kg/shared`
  - Add NestJS core packages as dependencies
  - Add TypeScript, `@nestjs/cli` as devDependencies
- [ ] **PS-RL-028**: Create `server/src/` directory for source files
- [ ] **PS-RL-029**: Create `server/src/main.ts` as the NestJS bootstrap entry
- [ ] **PS-RL-030**: Create `server/src/app.module.ts` as the root module
- [ ] **PS-RL-031**: Create `server/src/modules/` for feature modules
- [ ] **PS-RL-032**: Create `server/src/modules/auth/` for authentication module
- [ ] **PS-RL-033**: Create `server/src/modules/specs/` for spec CRUD module
- [ ] **PS-RL-034**: Create `server/src/modules/graph/` for knowledge graph module
- [ ] **PS-RL-035**: Create `server/src/modules/agent/` for agent orchestration module
- [ ] **PS-RL-036**: Create `server/src/modules/rag/` for RAG layer module
- [ ] **PS-RL-037**: Create `server/src/modules/git/` for git integration module
- [ ] **PS-RL-038**: Create `server/src/modules/users/` for user management module
- [ ] **PS-RL-039**: Create `server/src/common/` for shared server utilities
- [ ] **PS-RL-040**: Create `server/src/common/guards/` for NestJS guards
- [ ] **PS-RL-041**: Create `server/src/common/interceptors/` for NestJS interceptors
- [ ] **PS-RL-042**: Create `server/src/common/filters/` for exception filters
- [ ] **PS-RL-043**: Create `server/src/common/pipes/` for validation pipes
- [ ] **PS-RL-044**: Create `server/src/common/decorators/` for custom decorators
- [ ] **PS-RL-045**: Create `server/src/common/middleware/` for middleware
- [ ] **PS-RL-046**: Create `server/src/config/` for configuration modules
- [ ] **PS-RL-047**: Create `server/src/database/` for database setup and migrations
- [ ] **PS-RL-048**: Create `server/src/database/migrations/` for migration files
- [ ] **PS-RL-049**: Create `server/src/database/seeds/` for seed data
- [ ] **PS-RL-050**: Create `server/test/` for server integration tests

### 1.4 Knowledge Graph Directory (`knowledge-graph/`)

- [ ] **PS-RL-051**: Create `knowledge-graph/` directory at repo root
- [ ] **PS-RL-052**: Create `knowledge-graph/specs/` for individual spec JSON files
- [ ] **PS-RL-053**: Create `knowledge-graph/documents/` for spec document groupings
- [ ] **PS-RL-054**: Create `knowledge-graph/edges/` for edge definition files
- [ ] **PS-RL-055**: Create `knowledge-graph/meta/` for graph metadata (indexes, caches)
- [ ] **PS-RL-056**: Create `knowledge-graph/README.md` explaining the data format
  - Document the spec JSON schema
  - Document the edge JSON schema
  - Document the document grouping schema
  - Explain the git versioning strategy for this directory

#### Design Decisions

> **Q**: Should spec files be one JSON file per spec, or should specs be grouped by document (one JSON file per spec document containing all its specs)?
> **A**: One JSON file per spec. This minimizes git merge conflicts, enables per-spec `git log` history, and keeps files small and focused. A separate document index file (`documents/{doc-id}.json`) lists the ordered spec IDs belonging to that document. This separation means editing one spec doesn't touch other specs' files.

> **Q**: Should edges be stored in a separate directory (`edges/`), or alongside the specs they connect (e.g., in the source spec's directory)?
> **A**: Separate `edges/` directory with one JSON file per edge. Storing edges alongside specs creates ambiguity (which spec "owns" the edge?) and complicates queries for all edges of a given type. A dedicated `edges/` directory makes it easy to scan all edges, build adjacency lists, and manage edge lifecycle independently from specs.

> **Q**: Should the knowledge graph directory have a flat structure or a nested structure (e.g., `specs/{document-id}/{spec-id}.json`)?
> **A**: Nested structure: `specs/{document-id}/{spec-id}.json`. This groups related specs visually in the filesystem, makes directory listings meaningful, and keeps any single directory from growing to thousands of files. The document-level grouping also aligns with git diff views — changes to a document's specs appear together. Edge files stay flat: `edges/{edge-id}.json`.

> **Q**: What is the maximum file size concern? If a spec document grows very large, does the JSON file become unwieldy?
> **A**: Not a concern with one-file-per-spec. Individual spec JSON files will be 1–10KB (title, markdown content, metadata). Even large specs with extensive content won't exceed 50KB. The document index file is just an array of IDs — negligible size. Git handles this granularity efficiently. If a future spec type somehow exceeds 1MB, split its content into a separate markdown file referenced by the JSON.

> **Q**: Should there be an index file (e.g., `meta/index.json`) that maps spec IDs to file paths for fast lookups, or should the system scan the directory?
> **A**: Yes, maintain `meta/index.json` mapping spec IDs to file paths. Directory scanning is O(n) and hits the filesystem on every lookup. The index is rebuilt on server start by scanning, then kept in sync as specs are created/updated. The index file is git-tracked (it's derived but useful for quick startup). Also maintain `meta/documents.json` listing all document IDs.

> **Q**: Should edge indexes be maintained (e.g., an adjacency list file), or computed on demand?
> **A**: Maintain `meta/adjacency.json` — a precomputed adjacency list mapping each spec ID to its outgoing and incoming edges. This is the primary data structure for graph traversal and visualization. Recomputed on server start from the `edges/` directory, updated incrementally on edge mutations. Git-tracked for fast startup on clone. Graph crawling performance depends on this index.

> **Q**: Should the knowledge graph directory be its own git repo (submodule), or part of the main project repo? A separate repo allows independent version history but adds git complexity.
> **A**: Part of the main project repo. Submodules add friction to every git operation (clone, pull, branch, merge) and create synchronization headaches. The knowledge graph directory is just a folder in the monorepo with its own directory structure. Per-spec version history is available via `git log -- knowledge-graph/specs/{doc}/{spec}.json`. No submodule needed.

> **Q**: When the PRD says "each user project is a git repo" — does that mean the entire monorepo is the project, or just the knowledge graph directory?
> **A**: The entire monorepo is the project. Each user/team that creates a "project" in the platform gets a full git repo containing the application code, knowledge graph data, and generated UI projects. The monorepo IS the project. When the platform creates a new project, it initializes a new git repo from a template that includes the full directory structure.

> **Q**: How should the `.gitignore` within the knowledge graph directory be configured? Should generated metadata (indexes, caches) be committed or ignored?
> **A**: Commit the indexes (`meta/index.json`, `meta/adjacency.json`, `meta/documents.json`). Although they're derived, committing them avoids a rebuild-on-clone step and makes the repo self-contained. Ignore caches and temporary files: `meta/cache/`, `meta/*.tmp`. The `.gitignore` in `knowledge-graph/` should only ignore transient artifacts, not the structural metadata.

### 1.5 Shared Package (`shared/`)

- [ ] **PS-RL-057**: Create `shared/` directory as a workspace package
- [ ] **PS-RL-058**: Create `shared/package.json`
  - Set `"name"` to `@kg/shared`
  - Set `"type": "module"`
  - Set `"main"` and `"types"` entry points
  - Define scripts: `build`, `test`, `lint`
- [ ] **PS-RL-059**: Create `shared/src/` directory
- [ ] **PS-RL-060**: Create `shared/src/index.ts` as the barrel export
- [ ] **PS-RL-061**: Create `shared/src/types/` for shared type definitions
- [ ] **PS-RL-062**: Create `shared/src/types/spec.ts` for spec-related types
- [ ] **PS-RL-063**: Create `shared/src/types/edge.ts` for edge-related types
- [ ] **PS-RL-064**: Create `shared/src/types/graph.ts` for graph query/response types
- [ ] **PS-RL-065**: Create `shared/src/types/user.ts` for user-related types
- [ ] **PS-RL-066**: Create `shared/src/types/auth.ts` for auth-related types
- [ ] **PS-RL-067**: Create `shared/src/types/agent.ts` for agent session/message types
- [ ] **PS-RL-068**: Create `shared/src/types/plan.ts` for plan-related types
- [ ] **PS-RL-069**: Create `shared/src/types/api.ts` for API request/response envelope types
- [ ] **PS-RL-070**: Create `shared/src/types/websocket.ts` for WebSocket event types
- [ ] **PS-RL-071**: Create `shared/src/types/permissions.ts` for permission types
- [ ] **PS-RL-072**: Create `shared/src/types/version.ts` for version/diff types
- [ ] **PS-RL-073**: Create `shared/src/constants/` for shared constants
- [ ] **PS-RL-074**: Create `shared/src/constants/edge-types.ts` defining the 5 edge types
- [ ] **PS-RL-075**: Create `shared/src/constants/api-routes.ts` defining all API route constants
- [ ] **PS-RL-076**: Create `shared/src/constants/websocket-events.ts` defining WS event names
- [ ] **PS-RL-077**: Create `shared/src/utils/` for shared utility functions
- [ ] **PS-RL-078**: Create `shared/src/utils/validation.ts` for shared validation helpers
- [ ] **PS-RL-079**: Create `shared/src/utils/id.ts` for ID generation utilities
- [ ] **PS-RL-080**: Create `shared/src/errors/` for shared error types and codes

### 1.6 Packages Directory (`packages/`)

- [ ] **PS-RL-081**: Create `packages/` directory for internal packages
- [ ] **PS-RL-082**: Create `packages/mcp-servers/` directory
  - Will contain individual MCP server implementations
- [ ] **PS-RL-083**: Create `packages/mcp-servers/package.json`
  - Set `"name"` to `@kg/mcp-servers`
  - Set `"type": "module"`
- [ ] **PS-RL-084**: Create `packages/mcp-servers/src/` directory
- [ ] **PS-RL-085**: Create `packages/claude-code-wrapper/` directory
  - Will contain the Claude Code integration wrapper
- [ ] **PS-RL-086**: Create `packages/claude-code-wrapper/package.json`
  - Set `"name"` to `@kg/claude-code-wrapper`
  - Set `"type": "module"`
- [ ] **PS-RL-087**: Create `packages/claude-code-wrapper/src/` directory

### 1.7 Supporting Directories

- [ ] **PS-RL-088**: Create `scripts/` directory for build/dev/deploy scripts
- [ ] **PS-RL-089**: Create `scripts/dev.ts` — orchestrates `bun dev` across workspaces
- [ ] **PS-RL-090**: Create `scripts/build.ts` — orchestrates production builds
- [ ] **PS-RL-091**: Create `scripts/clean.ts` — removes build artifacts, node_modules
- [ ] **PS-RL-092**: Create `scripts/setup.ts` — first-time project setup (install, db init, env)
- [ ] **PS-RL-093**: Create `scripts/migrate.ts` — runs database migrations
- [ ] **PS-RL-094**: Create `config/` directory for shared configuration
- [ ] **PS-RL-095**: Create `plans/` directory (this directory) for plan documents
- [ ] **PS-RL-096**: Create `docs/` directory for generated documentation
- [ ] **PS-RL-097**: Create `.github/` directory for GitHub-specific config (if using GitHub)
- [ ] **PS-RL-098**: Create `.github/workflows/` for CI/CD workflow definitions

---

## 2. Bun Workspace Configuration

### 2.1 Root Workspace Setup

- [ ] **PS-WS-001**: Configure `"workspaces"` in root `package.json`
  - Include `"client"`, `"server"`, `"shared"`, `"packages/*"`
- [ ] **PS-WS-002**: Verify bun workspace resolution with `bun install`
  - Ensure `@kg/shared` resolves in both `@kg/client` and `@kg/server`
  - Ensure `@kg/mcp-servers` resolves in `@kg/server`
  - Ensure `@kg/claude-code-wrapper` resolves in `@kg/server`
- [ ] **PS-WS-003**: Configure workspace scripts in root `package.json`
  - `"dev"`: run client + server + any watchers in parallel
  - `"build"`: build shared → server → client (in dependency order)
  - `"test"`: run tests across all workspaces
  - `"lint"`: lint all workspaces
  - `"format"`: format all workspaces
  - `"clean"`: remove dist/, node_modules across all workspaces
- [ ] **PS-WS-004**: Add `"bun"` field to root `package.json` if needed for bun-specific config
- [ ] **PS-WS-005**: Test cross-workspace type imports
  - Import a type from `@kg/shared` in `@kg/client` and verify autocomplete
  - Import a type from `@kg/shared` in `@kg/server` and verify autocomplete

### 2.2 Dependency Management

- [ ] **PS-WS-006**: Define which dependencies belong at the root vs. workspace level
  - Root: TypeScript, ESLint, Prettier (shared tooling)
  - Client: React, Vite, SCSS (frontend-specific)
  - Server: NestJS, bcrypt, JWT libraries (backend-specific)
  - Shared: minimal (no heavy dependencies)
- [ ] **PS-WS-007**: Add `typescript` as a root devDependency
- [ ] **PS-WS-008**: Add `eslint` as a root devDependency
- [ ] **PS-WS-009**: Add `prettier` as a root devDependency
- [ ] **PS-WS-010**: Add `@types/bun` as a root devDependency
- [ ] **PS-WS-011**: Pin critical dependency versions for reproducibility
  - React, NestJS core, TypeScript should all be pinned
- [ ] **PS-WS-012**: Create `bunfig.toml` at the root if custom bun configuration is needed
  - Configure install behavior, registry, etc.

#### Design Decisions

> **Q**: Should workspace packages use a scoped name like `@kg/client`, or an unscoped name like `kg-client`? Scoped names prevent npm collisions but add verbosity to imports.
> **A**: Use scoped names: `@kg/client`, `@kg/server`, `@kg/shared`, etc. Scoped names are the modern standard for monorepo packages, prevent collisions, and make workspace references unambiguous. The verbosity cost is negligible since these are internal workspace references, not manually typed imports — IDEs auto-complete them.

> **Q**: What should the scope prefix be? `@kg` (knowledge graph)? `@botnet`? Something else? This affects every import and package.json across the repo.
> **A**: Use `@kg` (knowledge graph). It's short, descriptive, and directly reflects the core domain. `@botnet` is the parent project name but less descriptive for package scoping. Keep it concise — every import benefits from brevity. If the project is later published, `@kg` is generic enough to claim or alias.

> **Q**: Should the MCP servers be a single package (`@kg/mcp-servers`) or individual packages per server (`@kg/mcp-graph`, `@kg/mcp-rag`, `@kg/mcp-gen-ui`)? Individual packages enable independent deployment but add workspace complexity.
> **A**: Single package: `@kg/mcp-servers`. MCP servers share common utilities (connection handling, error formatting, tool registration patterns) and are deployed together. Individual packages add 3–5 workspace entries, separate tsconfigs, and coordination overhead for minimal benefit. Use internal directory structure (`src/graph/`, `src/rag/`, `src/gen-ui/`) with separate entry points per server.

> **Q**: Should the Claude Code wrapper be its own package, or part of the server? Separating it enables testing in isolation and potential reuse, but adds a package to maintain.
> **A**: Own package: `@kg/claude-code-wrapper`. The wrapper encapsulates process spawning, sandboxing, prompt construction, and output parsing — a clear boundary. Isolating it enables focused unit testing with mocked child processes, and it may be consumed by both the server and MCP servers. The maintenance cost of one extra package.json is trivial.

> **Q**: Should there be a separate `@kg/database` package for migrations, schemas, and query helpers, or should that live directly in the server?
> **A**: Keep it in the server (`server/src/database/`). The database is exclusively consumed by the NestJS server — no other package needs direct DB access. MCP servers and the Claude Code wrapper interact with the DB through the server's API. A separate package adds indirection with no consumer benefit. Migrations, seeds, and connection config stay server-internal.

---

## 3. Shared Types Package

### 3.1 Package Setup

- [ ] **PS-ST-001**: Configure `shared/tsconfig.json` to compile to ESM
  - Target: `ES2022` or later
  - Module: `ESNext`
  - ModuleResolution: `bundler`
  - Enable `declaration` and `declarationMap` for type exports
  - Set `outDir` to `dist/`
  - Set `rootDir` to `src/`
- [ ] **PS-ST-002**: Configure `shared/package.json` exports
  - Set `"exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } }`
  - Set `"files": ["dist"]`
- [ ] **PS-ST-003**: Add build script to generate declaration files
- [ ] **PS-ST-004**: Add a watch mode script for development

### 3.2 Core Type Definitions

- [ ] **PS-ST-005**: Define `Spec` interface in `shared/src/types/spec.ts`
  - `id: string`
  - `title: string`
  - `content: string` (markdown)
  - `authorId: string`
  - `createdAt: string` (ISO 8601)
  - `updatedAt: string` (ISO 8601)
  - `version: number`
  - `commitHash: string`
  - `tags: string[]`
  - `permissionLevel: PermissionLevel`
  - `summary?: string` (for restricted access)
  - `documentId: string`
  - `mediaAssociations: MediaRef[]`
- [ ] **PS-ST-006**: Define `SpecDocument` interface in `shared/src/types/spec.ts`
  - `id: string`
  - `title: string`
  - `description: string`
  - `specIds: string[]` (ordered)
  - `authorId: string`
  - `createdAt: string`
  - `updatedAt: string`
  - `commitHash: string`
- [ ] **PS-ST-007**: Define `Edge` interface in `shared/src/types/edge.ts`
  - `id: string`
  - `sourceSpecId: string`
  - `targetSpecId: string`
  - `type: EdgeType`
  - `metadata: Record<string, unknown>` (agent-embedded analysis)
  - `createdAt: string`
  - `createdBy: string` (user ID or "agent")
  - `commitHash: string`
- [ ] **PS-ST-008**: Define `EdgeType` enum in `shared/src/types/edge.ts`
  - `DERIVED_FROM`, `DEPENDS_ON`, `RELATED_TO`, `CONTRADICTS`, `SUPERSEDES`
- [ ] **PS-ST-009**: Define `GraphNode` interface in `shared/src/types/graph.ts`
  - Wraps a `Spec` with graph-specific metadata (position hints, display state)
- [ ] **PS-ST-010**: Define `GraphQuery` and `GraphResponse` types in `shared/src/types/graph.ts`
- [ ] **PS-ST-011**: Define `User` interface in `shared/src/types/user.ts`
  - `id: string`
  - `username: string`
  - `email: string`
  - `displayName: string`
  - `createdAt: string`
- [ ] **PS-ST-012**: Define `AuthPayload` and `JwtPayload` in `shared/src/types/auth.ts`
- [ ] **PS-ST-013**: Define `AgentSession` interface in `shared/src/types/agent.ts`
  - `sessionId: string`
  - `userId: string`
  - `agentType: AgentType`
  - `status: AgentSessionStatus`
  - `createdAt: string`
  - `context: AgentContext`
- [ ] **PS-ST-014**: Define `AgentMessage` interface in `shared/src/types/agent.ts`
  - `id: string`
  - `sessionId: string`
  - `role: 'user' | 'agent'`
  - `content: string`
  - `interactiveElements?: InteractiveElement[]`
  - `graphLinks?: GraphLink[]`
  - `timestamp: string`
- [ ] **PS-ST-015**: Define `AgentType` enum in `shared/src/types/agent.ts`
  - `KNOWLEDGE_GRAPH`, `DIALOG`, `GENERATIVE_UI`, `PLAN_GENERATOR`, `GRAPH_CRAWLER`
- [ ] **PS-ST-016**: Define `Plan` and `PlanStep` interfaces in `shared/src/types/plan.ts`
- [ ] **PS-ST-017**: Define `ApiResponse<T>` envelope type in `shared/src/types/api.ts`
  - `success: boolean`
  - `data?: T`
  - `error?: ApiError`
  - `pagination?: PaginationMeta`
- [ ] **PS-ST-018**: Define `ApiError` type in `shared/src/types/api.ts`
  - `code: string`
  - `message: string`
  - `details?: Record<string, unknown>`
- [ ] **PS-ST-019**: Define `PaginationMeta` type in `shared/src/types/api.ts`
  - `page: number`
  - `pageSize: number`
  - `total: number`
  - `totalPages: number`
- [ ] **PS-ST-020**: Define WebSocket event types in `shared/src/types/websocket.ts`
  - `AgentStatusUpdate`, `AgentMessageEvent`, `SpecChangeEvent`, `SyncEvent`
- [ ] **PS-ST-021**: Define `PermissionLevel` enum in `shared/src/types/permissions.ts`
  - `FULL_ACCESS`, `SUMMARY_ACCESS`
- [ ] **PS-ST-022**: Define `PermissionGrant` interface in `shared/src/types/permissions.ts`
  - `specId: string`
  - `userId: string`
  - `level: PermissionLevel`
  - `grantedBy: string`
  - `grantedAt: string`
  - `token: string`
- [ ] **PS-ST-023**: Define `SpecDiff` and `VersionInfo` types in `shared/src/types/version.ts`
- [ ] **PS-ST-024**: Define `MediaRef` type in `shared/src/types/spec.ts`
  - `id: string`
  - `type: 'image' | 'video' | 'audio' | 'document'`
  - `path: string`
  - `originalName: string`
- [ ] **PS-ST-025**: Define `InquiryQueueItem` type in `shared/src/types/graph.ts`
  - `id: string`
  - `type: 'edge-issue' | 'node-issue' | 'conflict' | 'suggestion'`
  - `description: string`
  - `relatedSpecIds: string[]`
  - `relatedEdgeIds: string[]`
  - `createdAt: string`
  - `status: 'pending' | 'reviewed' | 'resolved'`

### 3.3 Constants

- [ ] **PS-ST-026**: Define edge type constants in `shared/src/constants/edge-types.ts`
  - Export the 5 edge type values and their display labels
  - Export edge type descriptions for UI tooltips
- [ ] **PS-ST-027**: Define API route constants in `shared/src/constants/api-routes.ts`
  - All REST endpoints as template strings
  - Grouped by module (auth, specs, graph, agent, etc.)
- [ ] **PS-ST-028**: Define WebSocket event name constants in `shared/src/constants/websocket-events.ts`
- [ ] **PS-ST-029**: Define permission level constants in `shared/src/constants/permissions.ts`
- [ ] **PS-ST-030**: Define agent type constants in `shared/src/constants/agent-types.ts`

### 3.4 Shared Utilities

- [ ] **PS-ST-031**: Implement ID generation utility in `shared/src/utils/id.ts`
  - UUID v4 generation (or nanoid) for spec IDs, edge IDs, etc.
- [ ] **PS-ST-032**: Implement validation helpers in `shared/src/utils/validation.ts`
  - `isValidSpecId()`, `isValidEdgeType()`, `isValidPermissionLevel()`
- [ ] **PS-ST-033**: Implement date formatting utilities in `shared/src/utils/dates.ts`
- [ ] **PS-ST-034**: Define shared error codes in `shared/src/errors/error-codes.ts`
- [ ] **PS-ST-035**: Define custom error classes in `shared/src/errors/errors.ts`
  - `NotFoundError`, `ValidationError`, `AuthorizationError`, `ConflictError`

### 3.5 Barrel Exports

- [ ] **PS-ST-036**: Create barrel export `shared/src/types/index.ts`
  - Re-export all types
- [ ] **PS-ST-037**: Create barrel export `shared/src/constants/index.ts`
- [ ] **PS-ST-038**: Create barrel export `shared/src/utils/index.ts`
- [ ] **PS-ST-039**: Create barrel export `shared/src/errors/index.ts`
- [ ] **PS-ST-040**: Update `shared/src/index.ts` to re-export all barrels

#### Design Decisions

> **Q**: Should `@kg/shared` contain only types and constants, or also runtime utilities (validation functions, date formatters, ID generators)?
> **A**: Types, constants, AND lightweight runtime utilities. Validation functions (`isValidSpecId`, `isValidEdgeType`), ID generators (nanoid/UUID wrappers), and date formatters are used identically on client and server. Keeping them in shared eliminates duplication and ensures consistency. The key constraint: shared must have zero heavy dependencies — only pure TypeScript utilities.

> **Q**: Should shared API route constants include full URL builders (e.g., `getSpecUrl(id: string) => /api/v1/specs/${id}`), or just path templates?
> **A**: Full URL builder functions. They ensure client and server agree on route shapes and make refactoring safe (change the builder, all consumers update). Export both the template string (`SPEC_ROUTE = '/api/v1/specs/:id'`) and the builder function (`getSpecUrl(id) => \`/api/v1/specs/${id}\``). The template is useful for NestJS route decorators; the builder is useful for client fetch calls.

> **Q**: Should DTOs (Data Transfer Objects) live in shared (so the client can use them for request building), or only on the server?
> **A**: DTO interfaces (the shape) live in shared. DTO validation classes (with class-validator decorators) live on the server. The client imports the interface to build type-safe requests; the server imports both the interface and the decorated class for runtime validation. This gives type safety across the boundary without shipping server-side validation dependencies to the client.

> **Q**: Should shared types be compiled to JavaScript + declarations (requiring a build step), or should consuming packages import TypeScript source directly (no build needed but slower IDE)? Bun can import `.ts` directly, which simplifies things.
> **A**: Compile to JavaScript + declarations. Although Bun can import `.ts` directly, the client uses Vite (which has its own module resolution), and IDE performance degrades with direct `.ts` imports across packages. A build step (`tsc`) generates `.js` + `.d.ts` files, ensuring fast IDE resolution and compatibility with both Bun and Vite. The shared package runs `tsc --watch` during development.

> **Q**: If importing TypeScript source directly, should `shared/src/index.ts` be the entry point, or should consumers import specific files like `@kg/shared/types/spec`?
> **A**: Use `shared/src/index.ts` as the single barrel export entry point (compiled to `dist/index.js`). Consumers import from `@kg/shared` only. Sub-path imports like `@kg/shared/types/spec` add configuration complexity (package.json `exports` map) and break when internal structure changes. The barrel re-exports everything; tree-shaking in Vite's production build eliminates unused code.

> **Q**: What is the policy when shared code grows large? Should sub-packages be split off (e.g., `@kg/shared-types`, `@kg/shared-utils`), or should shared remain a single package with good barrel exports?
> **A**: Single package with organized internal directories and barrel exports. Split only if the shared package exceeds ~100 files or introduces heavy dependencies that shouldn't ship to all consumers. In practice, shared types/constants/utils for this project will stay well under that threshold. Use sub-directory barrels (`types/index.ts`, `utils/index.ts`) for internal organization.

> **Q**: Should there be a review gate before adding new items to shared, to prevent it from becoming a dumping ground?
> **A**: Yes — a lightweight one. The rule: code goes in shared only if it's consumed by at least 2 packages (client + server, or server + mcp-servers). If it's only used by one consumer, it belongs in that consumer's codebase. This is enforced via PR review convention, not a technical gate. Document the rule in `CONTRIBUTING.md`.

---

## 4. TypeScript Configuration

### 4.1 Base Configuration

- [ ] **PS-TS-001**: Create `tsconfig.base.json` at the repo root
  - `"compilerOptions.target"`: `"ES2022"`
  - `"compilerOptions.module"`: `"ESNext"`
  - `"compilerOptions.moduleResolution"`: `"bundler"`
  - `"compilerOptions.strict"`: `true`
  - `"compilerOptions.esModuleInterop"`: `true`
  - `"compilerOptions.skipLibCheck"`: `true`
  - `"compilerOptions.forceConsistentCasingInFileNames"`: `true`
  - `"compilerOptions.resolveJsonModule"`: `true`
  - `"compilerOptions.isolatedModules"`: `true`
  - `"compilerOptions.verbatimModuleSyntax"`: `true`
  - `"compilerOptions.noUncheckedIndexedAccess"`: `true`
  - `"compilerOptions.noImplicitOverride"`: `true`
  - `"compilerOptions.exactOptionalPropertyTypes"`: `true`

### 4.2 Client Configuration

- [ ] **PS-TS-002**: Create `client/tsconfig.json` extending the base
  - Extend `../tsconfig.base.json`
  - Add `"compilerOptions.jsx"`: `"react-jsx"`
  - Add `"compilerOptions.lib"`: `["ES2022", "DOM", "DOM.Iterable"]`
  - Add `"compilerOptions.outDir"`: `"dist"`
  - Add path aliases: `"@ui/*"` → `"ui/src/*"`, `"@shared"` → `"../shared/src"`
  - Set `"include"`: `["ui/src/**/*"]`
  - Set `"exclude"`: `["node_modules", "dist"]`
- [ ] **PS-TS-003**: Create `client/tsconfig.node.json` for Vite config files
  - Target Node.js environment for `vite.config.ts`
  - `"compilerOptions.composite"`: `true`

### 4.3 Server Configuration

- [ ] **PS-TS-004**: Create `server/tsconfig.json` extending the base
  - Extend `../tsconfig.base.json`
  - Add `"compilerOptions.emitDecoratorMetadata"`: `true` (NestJS requirement)
  - Add `"compilerOptions.experimentalDecorators"`: `true` (NestJS requirement)
  - Add `"compilerOptions.outDir"`: `"dist"`
  - Add path aliases: `"@modules/*"` → `"src/modules/*"`, `"@common/*"` → `"src/common/*"`, `"@config/*"` → `"src/config/*"`, `"@shared"` → `"../shared/src"`
  - Set `"include"`: `["src/**/*"]`
  - Set `"exclude"`: `["node_modules", "dist", "test"]`
- [ ] **PS-TS-005**: Create `server/tsconfig.build.json` for production builds
  - Exclude test files and spec files
  - Enable `"declaration"`: `true`

### 4.4 Shared Configuration

- [ ] **PS-TS-006**: Create `shared/tsconfig.json` extending the base
  - Extend `../tsconfig.base.json`
  - Enable `"declaration"`: `true`
  - Enable `"declarationMap"`: `true`
  - Set `"outDir"`: `"dist"`
  - Set `"rootDir"`: `"src"`
  - Set `"include"`: `["src/**/*"]`

### 4.5 Package Configurations

- [ ] **PS-TS-007**: Create `packages/mcp-servers/tsconfig.json` extending the base
  - NestJS-compatible (decorators)
  - Path alias to `@shared`
- [ ] **PS-TS-008**: Create `packages/claude-code-wrapper/tsconfig.json` extending the base
  - Path alias to `@shared`

### 4.6 Root References

- [ ] **PS-TS-009**: Create `tsconfig.json` at the repo root (project references)
  - Use `"references"` to point to client, server, shared, packages/*
  - This enables IDE-wide type checking and go-to-definition
- [ ] **PS-TS-010**: Verify TypeScript project references resolve correctly
  - Run `tsc --build` from root and confirm no errors
  - Verify that changing a shared type propagates to both client and server

---

## 5. Vite Configuration

### 5.1 Client Vite Config

- [ ] **PS-VT-001**: Create `client/vite.config.ts`
  - Import and configure `@vitejs/plugin-react`
  - Set `root` to `ui/`
  - Configure `resolve.alias` matching tsconfig path aliases
  - Set `server.port` to a configurable value (default 3000)
  - Set `server.proxy` to forward `/api` and `/ws` to the NestJS server
  - Configure `build.outDir` to `../dist`
  - Set `build.sourcemap` to `true` for development
- [ ] **PS-VT-002**: Configure SCSS support in Vite
  - Ensure `css.preprocessorOptions.scss` is set
  - Add global SCSS imports (variables, mixins) via `additionalData`
- [ ] **PS-VT-003**: Configure environment variable handling
  - Define `envPrefix` as `"VITE_"` (default)
  - Create `client/ui/src/env.d.ts` with `ImportMetaEnv` type declarations
- [ ] **PS-VT-004**: Configure chunk splitting strategy
  - Vendor chunks for React, large libraries
  - Lazy-loaded route chunks
- [ ] **PS-VT-005**: Add Vite plugin for SVG imports (if needed)
- [ ] **PS-VT-006**: Configure `optimizeDeps.include` for pre-bundling workspace packages
  - Include `@kg/shared` to ensure proper pre-bundling

### 5.2 Generative UI Vite Template

- [ ] **PS-VT-007**: Create a Vite config template for generative UI projects
  - Minimal configuration for ESM output
  - Configured for iframe-safe output (no external dependencies on host)
  - Build output to `dist/` within the gen project
- [ ] **PS-VT-008**: Create a minimal `package.json` template for gen UI projects
  - Only React and Vite as dependencies
  - Build script for generating dist/
- [ ] **PS-VT-009**: Document the gen UI project scaffold process in the gen README

#### Design Decisions

> **Q**: What is the minimal viable structure for a gen UI project? Is it: `package.json` + `vite.config.ts` + `index.html` + `src/main.tsx`? Or even simpler?
> **A**: Exactly that minimal set: `package.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, and `src/App.tsx`. The Vite config is templated (minimal, ESM output, React plugin). The agent generates the React components in `src/`. This is the simplest structure that supports JSX, hot reload during development, and a production build for iframe loading.

> **Q**: Should gen UI projects use React (same as the main UI), or should they be framework-agnostic (vanilla JS/TS)?
> **A**: React. Using the same framework as the main UI means the agent has a consistent mental model, shared component patterns, and predictable output. Framework-agnostic adds complexity to the generation process without benefit. React is specified in the gen UI template's `package.json`. The agent generates React components and hooks.

> **Q**: Should gen UI projects have access to the shared types package, or should they be fully independent?
> **A**: Fully independent. Gen UI projects are sandboxed in iframes and must not depend on monorepo internals. If they need type information (e.g., spec shapes), the agent copies the relevant type definitions into the gen project. This ensures gen UIs are self-contained and portable — they work even if extracted from the monorepo.

> **Q**: Should gen UI projects have their own `node_modules`, or share dependencies with the main client?
> **A**: Their own `node_modules`. Gen UI projects are isolated — they run in iframes and build independently. Sharing dependencies with the main client creates coupling and version conflicts. Each gen project's `bun install` creates a local `node_modules`. The `node_modules` directories of gen projects are `.gitignore`-d (only `package.json` + `bun.lock` are committed).

> **Q**: If gen UI projects are workspace packages, they add weight to every `bun install`. If not, how are their dependencies installed?
> **A**: Gen UI projects are NOT workspace packages. Their dependencies are installed on-demand: when the agent creates a gen UI project, it runs `bun install` in that project's directory as part of the scaffolding step. When a user clones the repo, a setup script (or the server on first load) runs `bun install` in any gen projects that exist. A `scripts/install-gen-uis.ts` handles batch installation.

> **Q**: Should there be a fixed set of allowed dependencies for gen UI projects (for security), or can the agent install anything?
> **A**: Fixed allowlist of approved dependencies. Gen UI projects run in iframes with CSP restrictions, but supply-chain attacks are still a concern. Maintain an allowlist in `config/gen-ui-allowed-deps.json` containing: React, ReactDOM, common UI libraries (e.g., recharts, framer-motion), and utility libraries (lodash, date-fns). The agent's MCP tool enforces the allowlist when adding dependencies.

> **Q**: Should gen UI projects be pre-built (agent builds, dist/ is committed), or built on-demand when the user loads them?
> **A**: Pre-built by the agent. When the agent generates a gen UI project, it also runs the build and commits the `dist/` output. This means the user sees the result instantly without waiting for a build. The `dist/` directory is git-tracked (it's the deliverable). If the user modifies the source, they can rebuild manually or via an agent command.

> **Q**: How is the gen UI project's `dist/` served to the iframe? Through the main server, or does each gen project run its own dev server?
> **A**: Through the main server. The NestJS server serves static files from `client/gen/{user}/{project}/dist/` via a dedicated route: `GET /gen/:user/:project/*`. The iframe's `src` points to this route. No per-project dev server — that doesn't scale. During development, the agent can run a Vite dev server for live preview, but the committed `dist/` is the production serving mechanism.

> **Q**: Should gen UI builds be validated (type-check passes, no build errors) before being made available to the user?
> **A**: Yes. The agent's build step should include `tsc --noEmit` (type check) and `vite build` (bundle). If either fails, the agent reports the error in the chat dialog and doesn't commit the `dist/`. The user sees the error and can ask the agent to fix it. This prevents broken gen UIs from being served. Build validation is part of the agent's MCP tool workflow, not a separate step.

---

## 6. ESLint Configuration

### 6.1 Root ESLint Config

- [ ] **PS-EL-001**: Create `eslint.config.js` (flat config format) at the repo root
  - Use the new ESLint flat config (ESLint 9+)
  - Import `@typescript-eslint/eslint-plugin`
  - Import `@typescript-eslint/parser`
  - Configure global ignores: `node_modules`, `dist`, `*.config.*`
- [ ] **PS-EL-002**: Configure TypeScript rules
  - `@typescript-eslint/no-unused-vars` with `argsIgnorePattern: "^_"`
  - `@typescript-eslint/explicit-function-return-type`: warn (with overrides for React components)
  - `@typescript-eslint/no-explicit-any`: warn
  - `@typescript-eslint/consistent-type-imports`: enforce `type` imports
  - `@typescript-eslint/no-floating-promises`: error
- [ ] **PS-EL-003**: Configure general rules
  - `no-console`: warn (allow `console.error`, `console.warn`)
  - `eqeqeq`: error
  - `no-var`: error
  - `prefer-const`: error
  - `no-duplicate-imports`: error
- [ ] **PS-EL-004**: Add `eslint-plugin-import` for import ordering
  - Enforce import order: builtin → external → internal → parent → sibling
  - Enforce newline between import groups

### 6.2 Client-Specific ESLint

- [ ] **PS-EL-005**: Add React-specific ESLint configuration
  - Import `eslint-plugin-react`
  - Import `eslint-plugin-react-hooks`
  - `react-hooks/rules-of-hooks`: error
  - `react-hooks/exhaustive-deps`: warn
  - `react/jsx-no-target-blank`: error
  - `react/self-closing-comp`: warn
- [ ] **PS-EL-006**: Add accessibility ESLint rules
  - Import `eslint-plugin-jsx-a11y`
  - Enable recommended rules

### 6.3 Server-Specific ESLint

- [ ] **PS-EL-007**: Add NestJS-specific overrides if applicable
  - Allow decorators on class properties
  - Allow empty constructors (dependency injection)

### 6.4 Install Dependencies

- [ ] **PS-EL-008**: Install all ESLint-related dependencies at root
  - `eslint`
  - `@typescript-eslint/eslint-plugin`
  - `@typescript-eslint/parser`
  - `eslint-plugin-react`
  - `eslint-plugin-react-hooks`
  - `eslint-plugin-jsx-a11y`
  - `eslint-plugin-import`

---

## 7. Prettier Configuration

- [ ] **PS-PR-001**: Create `.prettierrc` at the repo root
  - `"semi"`: based on team preference (recommend `true`)
  - `"singleQuote"`: `true`
  - `"trailingComma"`: `"all"`
  - `"printWidth"`: `80`
  - `"tabWidth"`: `2`
  - `"useTabs"`: `false`
  - `"bracketSpacing"`: `true`
  - `"arrowParens"`: `"always"`
  - `"endOfLine"`: `"lf"`
- [ ] **PS-PR-002**: Create `.prettierignore` at the repo root
  - Ignore `node_modules`, `dist`, `bun.lock`, `*.md` (optional), `knowledge-graph/`
- [ ] **PS-PR-003**: Add Prettier as a devDependency at the root
- [ ] **PS-PR-004**: Add `prettier-plugin-organize-imports` if desired
- [ ] **PS-PR-005**: Verify Prettier and ESLint don't conflict
  - Install `eslint-config-prettier` to disable conflicting rules
  - Add it to the ESLint flat config

---

## 8. SCSS / Styling Configuration

### 8.1 SCSS Architecture

- [ ] **PS-SC-001**: Create `client/ui/src/styles/_variables.scss`
  - Define color tokens (primary, secondary, neutral, semantic)
  - Define spacing scale (4px base grid)
  - Define typography scale (font sizes, weights, line heights)
  - Define border radius values
  - Define shadow values
  - Define z-index scale
  - Define breakpoints for responsive design
- [ ] **PS-SC-002**: Create `client/ui/src/styles/_mixins.scss`
  - Responsive breakpoint mixins (`@mixin respond-to($breakpoint)`)
  - Typography mixins (`@mixin heading-1`, `@mixin body-text`)
  - Flexbox helper mixins
  - Grid helper mixins
  - Truncation mixin
  - Focus ring mixin (accessibility)
- [ ] **PS-SC-003**: Create `client/ui/src/styles/_reset.scss`
  - CSS reset / normalize styles
  - Box-sizing: border-box globally
  - Remove default margins/padding
  - Sensible defaults for typography
- [ ] **PS-SC-004**: Create `client/ui/src/styles/_animations.scss`
  - Define reusable keyframe animations
  - Transition timing function variables
  - Reduced motion media query wrapper
- [ ] **PS-SC-005**: Create `client/ui/src/styles/_utilities.scss`
  - Utility classes for common patterns (sr-only, clearfix, etc.)
- [ ] **PS-SC-006**: Create `client/ui/src/styles/global.scss`
  - Import reset, variables, mixins, animations, utilities
  - Set base html/body styles
  - Define CSS custom properties from SCSS variables (for runtime theming)
- [ ] **PS-SC-007**: Import `global.scss` in `client/ui/src/main.tsx`

### 8.2 BEM Convention Enforcement

- [ ] **PS-SC-008**: Document the BEM naming convention in a `STYLING.md` guide
  - Block: `PascalCase` (e.g., `SpecEditor`)
  - Element: `PascalCase__Element` (e.g., `SpecEditor__Header`)
  - Modifier: `PascalCase--modifier` (e.g., `SpecEditor--active`)
  - Examples for common patterns
- [ ] **PS-SC-009**: Create a component SCSS template file
  - Show the expected file naming: `ComponentName.module.scss` or `ComponentName.scss`
  - Show BEM structure with nesting
- [ ] **PS-SC-010**: Decide on CSS Modules vs. global BEM classes
  - If CSS Modules: configure Vite's `css.modules` options
  - If global: ensure BEM discipline via code review and linting
- [ ] **PS-SC-011**: Add `stylelint` for SCSS linting (optional but recommended)
  - Install `stylelint`, `stylelint-config-standard-scss`
  - Configure rules for BEM naming pattern validation
  - Add lint script to root package.json

---

## 9. Environment Configuration

### 9.1 Environment Files

- [ ] **PS-ENV-001**: Create `.env.example` at the repo root
  - Document every environment variable with descriptions
  - Include placeholder values
  - Group by concern: database, auth, agent, features
- [ ] **PS-ENV-002**: Define server environment variables
  - `DATABASE_URL` — PostgreSQL connection string
  - `DATABASE_HOST`, `DATABASE_PORT`, `DATABASE_NAME`, `DATABASE_USER`, `DATABASE_PASSWORD`
  - `JWT_SECRET` — secret for signing JWTs
  - `JWT_EXPIRATION` — token expiration (e.g., "7d")
  - `PORT` — server port (default 4000)
  - `CORS_ORIGIN` — allowed CORS origins
  - `NODE_ENV` — environment (development, production, test)
  - `CLAUDE_CODE_BINARY_PATH` — path to Claude Code CLI executable
  - `ANTHROPIC_API_KEY` — API key passed to Claude Code subprocess (not used by server directly)
  - `CLAUDE_CODE_MAX_CONCURRENT` — max concurrent Claude Code processes
  - `CLAUDE_CODE_TIMEOUT_MS` — execution timeout per invocation (ms)
  - `EMBEDDING_PROVIDER` — "local" (default) or "openai"
  - `LOCAL_EMBEDDING_MODEL_PATH` — path to local ONNX/GGUF model
  - `LOCAL_EMBEDDING_MODEL_NAME` — local model identifier for provenance
  - `EMBEDDING_API_KEY` — OpenAI API key (only when provider=openai)
  - `EMBEDDING_MODEL` — OpenAI model identifier (only when provider=openai)
  - `EMBEDDING_DIMENSIONS` — vector dimensions (must match model output)
  - `GIT_USER_NAME` — git committer name
  - `GIT_USER_EMAIL` — git committer email
  - `LOG_LEVEL` — logging verbosity (debug, info, warn, error)
- [ ] **PS-ENV-003**: Define client environment variables (VITE_ prefixed)
  - `VITE_API_BASE_URL` — API base URL
  - `VITE_WS_URL` — WebSocket URL
  - `VITE_APP_NAME` — application display name
- [ ] **PS-ENV-004**: Create `.env.test` template for test environments
  - Use in-memory or test-specific database
  - Mock API keys
- [ ] **PS-ENV-005**: Create `.env.development` template for local development

### 9.2 Type-Safe Config

- [ ] **PS-ENV-006**: Create `server/src/config/configuration.ts`
  - Export a configuration factory function
  - Validate all required environment variables on startup
  - Return a typed configuration object
- [ ] **PS-ENV-007**: Create `server/src/config/config.module.ts`
  - Use NestJS `ConfigModule.forRoot()` with the factory
  - Set `isGlobal: true` for application-wide access
- [ ] **PS-ENV-008**: Create `server/src/config/config.validation.ts`
  - Use `class-validator` and `class-transformer` (or Zod) for env validation
  - Define a schema class with decorators for each env var
  - Throw descriptive errors on missing/invalid values
- [ ] **PS-ENV-009**: Create `client/ui/src/config.ts`
  - Type-safe access to `import.meta.env` variables
  - Provide defaults for optional values
  - Validate required values at app startup

#### Design Decisions

> **Q**: Should `.env` files be committed (with placeholder values) or always generated from `.env.example`? The PRD implies secrets are never committed.
> **A**: Never commit `.env` files. Commit `.env.example` with placeholder values and descriptions. Developers copy `.env.example` to `.env` on setup (the setup script automates this). `.env` is in `.gitignore`. This is the standard practice — secrets never enter git history. The `.env.example` serves as documentation for required variables.

> **Q**: Should there be separate `.env` files per workspace (`client/.env`, `server/.env`), or a single `.env` at the root?
> **A**: Single `.env` at the root. Most environment variables are server-side; the few client-side variables (prefixed `VITE_`) are read by Vite from the root or client directory. A single file avoids confusion about which `.env` holds which value. The NestJS ConfigModule and Vite both support reading from a root `.env`. Less files = less maintenance.

> **Q**: Should environment configuration support multiple profiles (development, staging, production, test) via separate files or a single file with overrides?
> **A**: Separate files: `.env.example` (template), `.env` (local development, gitignored), `.env.test` (test overrides, committed with safe values). Staging and production environments use their hosting platform's env var mechanism (CI secrets, cloud config), not committed files. The server loads `.env` by default and `.env.test` when `NODE_ENV=test`.

> **Q**: Should the server crash on startup if a required environment variable is missing, or should it log a warning and use a default?
> **A**: Crash on startup for required variables (DATABASE_URL, JWT_SECRET). Log a warning and use defaults for optional variables (LOG_LEVEL defaults to "info", PORT defaults to 4000, EMBEDDING_PROVIDER defaults to "local"). ANTHROPIC_API_KEY is only required if agent features are used — the server can start without it and disable the agent system. Crashing fast is better than running in a broken state. The error message should clearly list which variables are missing and reference `.env.example`.

> **Q**: Should configuration be validated using Zod, class-validator (NestJS convention), or a simpler approach?
> **A**: Use Zod for env validation. It's simpler than class-validator for this use case (no decorators needed, better TypeScript inference), works identically in server and shared packages, and produces clear error messages. NestJS's ConfigModule supports custom validation functions — pass a Zod schema's `.parse()` as the validator. Keep class-validator for DTO validation in NestJS controllers.

> **Q**: Should secrets be managed via environment variables only, or should a secrets manager (e.g., AWS Secrets Manager, Vault) be supported?
> **A**: Environment variables only through Phase 4. For local dev and initial deployment, `.env` files and CI secrets are sufficient. A secrets manager is Phase 5 production infrastructure — add support when deploying to cloud. The architecture should be ready (config loaded from env vars), but don't integrate Vault or AWS Secrets Manager until there's a production environment to protect.

> **Q**: How should the JWT secret be rotated? Does the system need to support multiple active secrets for zero-downtime rotation?
> **A**: Support two active secrets for rotation. On rotation: add the new secret as primary, keep the old secret as a verification fallback for existing tokens. JWT verification tries the primary secret first, then the fallback. After the old tokens' max expiry (e.g., 7 days), remove the fallback. This enables zero-downtime rotation. Implement in Phase 3 (auth hardening).

---

## 10. Git Configuration

### 10.1 Gitignore

- [ ] **PS-GIT-001**: Create `.gitignore` at the repo root
  - **Node/Bun**: `node_modules/`, `bun.lock` (if not committed), `.bun/`
  - **Build artifacts**: `dist/`, `build/`, `*.tsbuildinfo`
  - **Environment**: `.env`, `.env.local`, `.env.*.local`
  - **IDE**: `.vscode/settings.json` (keep `.vscode/extensions.json`), `.idea/`
  - **OS**: `.DS_Store`, `Thumbs.db`
  - **Logs**: `*.log`, `logs/`
  - **Testing**: `coverage/`, `.nyc_output/`
  - **Docker**: `.docker/`
  - **Generated**: `client/gen/*/dist/`
  - **Database**: `*.sqlite`, `*.sqlite3`
  - **Temp**: `tmp/`, `temp/`

### 10.2 Git Hooks

- [ ] **PS-GIT-002**: Set up git hooks mechanism
  - Option A: Use `lefthook` (Bun-compatible, fast)
  - Option B: Use `husky` (widely known)
  - Option C: Use simple shell scripts in `.githooks/`
  - Install chosen tool and add to devDependencies
- [ ] **PS-GIT-003**: Configure `pre-commit` hook
  - Run `lint-staged` to lint and format only staged files
  - Fail the commit if linting errors exist
- [ ] **PS-GIT-004**: Configure `lint-staged` (or equivalent)
  - `"*.{ts,tsx}"`: `["eslint --fix", "prettier --write"]`
  - `"*.{scss,css}"`: `["prettier --write"]`
  - `"*.{json,md}"`: `["prettier --write"]`
- [ ] **PS-GIT-005**: Configure `commit-msg` hook
  - Enforce conventional commit format: `type(scope): description`
  - Types: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `ci`
  - Use `commitlint` with `@commitlint/config-conventional`
- [ ] **PS-GIT-006**: Configure `pre-push` hook (optional)
  - Run full test suite before push
  - Run type checking (`tsc --noEmit`) before push

### 10.3 Git Attributes

- [ ] **PS-GIT-007**: Create `.gitattributes` at the repo root
  - `* text=auto eol=lf` — normalize line endings
  - `*.ts linguist-language=TypeScript`
  - `*.tsx linguist-language=TypeScript`
  - `*.scss linguist-language=SCSS`
  - `bun.lock binary` — treat lockfile as binary for diffs
  - `knowledge-graph/**/*.json merge=union` (or custom merge driver)

### 10.4 Branch Strategy

- [ ] **PS-GIT-008**: Document the branching strategy in `CONTRIBUTING.md`
  - `main` — production-ready code
  - `develop` — integration branch for features
  - `feature/*` — feature branches
  - `fix/*` — bug fix branches
  - `release/*` — release preparation branches
- [ ] **PS-GIT-009**: Configure branch protection rules (document what to set up)
  - `main`: require PR review, passing CI, no force push
  - `develop`: require passing CI

---

## 11. Docker Setup

### 11.1 Development Docker Compose

- [ ] **PS-DK-001**: Create `docker-compose.yml` at the repo root
  - Define `postgres` service
    - Image: `postgres:16` (or latest LTS)
    - Port: `5432:5432`
    - Environment: `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`
    - Volume: `postgres-data:/var/lib/postgresql/data`
    - Healthcheck: `pg_isready`
  - Define named volume `postgres-data` for persistence
- [ ] **PS-DK-002**: Add optional `pgadmin` service for database GUI
  - Image: `dpage/pgadmin4`
  - Port: `5050:80`
  - Environment: `PGADMIN_DEFAULT_EMAIL`, `PGADMIN_DEFAULT_PASSWORD`
- [ ] **PS-DK-003**: Add optional `redis` service (if caching/sessions need it later)
  - Image: `redis:7-alpine`
  - Port: `6379:6379`
- [ ] **PS-DK-004**: Create `docker-compose.test.yml` for test environment
  - Separate PostgreSQL instance with test database
  - Ephemeral (no volume persistence)

### 11.2 Production Dockerfiles (Scaffold Only)

- [ ] **PS-DK-005**: Create `server/Dockerfile` (scaffold)
  - Multi-stage build: install → build → runtime
  - Base image: `oven/bun:latest` (or specific version)
  - Copy workspace files respecting monorepo structure
  - Build the server
  - Expose port
  - CMD: `bun run start`
- [ ] **PS-DK-006**: Create `client/Dockerfile` (scaffold)
  - Multi-stage build: install → build → serve
  - Build the Vite app
  - Serve with nginx or a static file server
- [ ] **PS-DK-007**: Create `.dockerignore` at the repo root
  - Ignore: `node_modules`, `.git`, `dist`, `.env*`, `*.md`, `plans/`, `docs/`

#### Design Decisions

> **Q**: Should the development Docker Compose only include infrastructure (PostgreSQL, Redis), or should it also containerize the application services?
> **A**: Infrastructure only: PostgreSQL (and optionally Redis, pgAdmin). Application services (server, client) run natively on the host via `bun dev`. Containerizing application services in development adds rebuild latency, complicates debugging, and prevents hot reload. Docker is for stateful services that benefit from isolated persistence. Application code runs natively for the best DX.

> **Q**: Should there be a `docker-compose.override.yml` for local customizations?
> **A**: Yes. Create `docker-compose.override.yml.example` with common customizations (port changes, pgAdmin, Redis). Developers copy and modify it as `docker-compose.override.yml` (gitignored). Docker Compose auto-merges override files. This lets developers customize port mappings, add debugging tools, or enable optional services without modifying the committed compose file.

> **Q**: Should the production Docker image use `oven/bun` as the base image, or a more traditional Node.js image? Bun's Docker image is less battle-tested.
> **A**: Use `oven/bun:1-debian` (Debian-based, not Alpine — NestJS has native dependencies). Bun is the project's runtime, so the Docker image should match. Pin a specific Bun version tag for reproducibility. If Bun's Docker image causes issues in production, fall back to `node:20-slim` and run the `tsc`-compiled output. Test the production image in Phase 5 staging.

> **Q**: Should client and server be in the same Docker image, or separate images? Separate allows independent scaling but adds deployment complexity.
> **A**: Separate images. The client is a static asset bundle served by nginx; the server is a long-running NestJS process. They have completely different runtime characteristics, scaling needs, and update frequencies. Two images: `kg-client` (nginx + built assets) and `kg-server` (Bun + NestJS). A single docker-compose (or k8s config) ties them together in deployment.

> **Q**: Should the client be served by nginx, the NestJS server, or a dedicated static file server?
> **A**: Nginx in production, NestJS proxy in development. Nginx is purpose-built for serving static files with gzip, caching headers, and reverse proxy to the API server. In development, Vite's dev server handles the client with HMR, and the Vite proxy forwards API requests to NestJS. The production nginx config includes a `location /api/` block that proxies to the NestJS container.

---

## 12. Build System

### 12.1 Build Order

- [ ] **PS-BD-001**: Define the build dependency graph
  - `shared` must build first (produces declaration files)
  - `packages/*` build next (depend on shared)
  - `server` builds next (depends on shared, packages)
  - `client` builds last (depends on shared)
- [ ] **PS-BD-002**: Implement build orchestration in root `package.json`
  - Use `bun run --filter` or a script to build in order
  - `"build"`: `"bun run --filter @kg/shared build && bun run --filter @kg/server build && bun run --filter @kg/client build"`
- [ ] **PS-BD-003**: Alternatively, create `scripts/build.ts` for complex orchestration
  - Detect which packages have changed (for incremental builds)
  - Build only affected packages
  - Print build times per package

### 12.2 Development Build

- [ ] **PS-BD-004**: Configure `shared` package watch mode
  - Rebuild on file changes, emitting fresh declarations
  - Use `tsc --watch` or `bun build --watch`
- [ ] **PS-BD-005**: Configure Vite dev server for client
  - HMR enabled
  - Proxy API requests to server
- [ ] **PS-BD-006**: Configure server dev mode
  - Use `bun --watch src/main.ts` for auto-restart on changes
  - Alternatively use `nodemon` with bun
- [ ] **PS-BD-007**: Create `scripts/dev.ts` to orchestrate parallel dev servers
  - Start shared in watch mode
  - Start server in watch mode
  - Start client Vite dev server
  - Use `concurrently` or native child process management
  - Color-coded output per workspace

### 12.3 Production Build

- [ ] **PS-BD-008**: Configure client production build
  - Vite build with minification
  - Source maps (separate files, not inline)
  - Asset hashing for cache busting
  - Analyze bundle size (`rollup-plugin-visualizer` optional)
- [ ] **PS-BD-009**: Configure server production build
  - TypeScript compilation to JavaScript
  - Tree-shaking if possible
  - No source maps in production (or separate)
- [ ] **PS-BD-010**: Configure shared package production build
  - Emit only declarations and compiled JS
  - No tests or test fixtures in output

#### Design Decisions

> **Q**: Should the codebase be written to work with both Bun and Node.js, or commit fully to Bun-only? Bun-only simplifies things but creates a hard dependency on a less mature runtime.
> **A**: Write code using standard Node.js APIs, run on Bun. This gives Bun-primary execution with Node.js as a fallback. Avoid Bun-specific APIs in production code (shared, server, client). Use Bun-specific features only in scripts and the test runner (`bun test`). If Bun breaks NestJS, the server can switch to Node.js with zero code changes — only the run command changes.

> **Q**: NestJS has some Node.js-specific assumptions. Has Bun + NestJS been validated? If there are compatibility issues, is the fallback to use Node.js for the server and Bun for everything else?
> **A**: Bun + NestJS has been reported working in the community but is not officially supported by NestJS. Validate in the first 2 days of Phase 1 with a spike: scaffold a NestJS app, add decorators, guards, and a TypeORM/Drizzle connection, and run under Bun. If it works, proceed. If it fails, fall back to Node.js for the server only. This is the expected fallback — the rest of the monorepo stays on Bun.

> **Q**: Should CI run tests on both Bun and Node.js to catch compatibility issues early?
> **A**: No. Run CI on Bun only (the primary runtime). Dual-runtime testing doubles CI time for a theoretical benefit. If the Node.js fallback is triggered for the server, switch that CI job to Node.js. The codebase uses standard APIs, so compatibility issues are rare. Focus CI time on actual test coverage, not runtime matrix testing.

> **Q**: Should Vite also be used for the server build, or should the server use `tsc` directly (or `bun build`)?
> **A**: Server uses `tsc` for production builds. In development, Bun runs TypeScript directly (no build step). Vite is a frontend tool optimized for browser bundles — it's the wrong tool for a NestJS server. `tsc` produces clean ESM output with declaration files, which is exactly what the server needs. `bun build` is an option but `tsc` is safer for NestJS's decorator metadata.

> **Q**: Should the shared package use `tsc` for compilation, or `bun build`? `tsc` produces better declaration files; `bun build` is faster.
> **A**: Use `tsc`. Declaration files (`.d.ts`) are essential for IDE support across workspace boundaries. `bun build` doesn't emit declarations. The shared package is small enough that `tsc` speed is irrelevant (< 1 second). In watch mode, `tsc --watch` provides instant rebuilds. Correctness of type exports is more important than build speed for this package.

> **Q**: Is a build orchestrator (Turborepo, Nx) needed, or is `bun run --filter` sufficient? Turborepo adds caching and task graph optimization but adds complexity.
> **A**: `bun run --filter` is sufficient for now. The monorepo has ~5 packages with a simple dependency chain (shared → server, shared → client, shared → mcp-servers). Turborepo's caching benefits are marginal at this scale. If builds exceed 2 minutes or the package count grows past 10, reconsider. Use a simple `scripts/build.ts` for ordering instead of adding a build framework dependency.

> **Q**: For the server, should we use `bun --watch` (built-in), `nodemon`, or NestJS's `@nestjs/cli start --watch`? Each has different file watching behavior and restart speeds.
> **A**: Use `bun --watch src/main.ts`. It's built-in, fast, and doesn't require additional dependencies. NestJS's `--watch` uses webpack under the hood (slow, unnecessary with Bun). Nodemon adds a dependency for something Bun provides natively. `bun --watch` restarts the process on any `.ts` file change in the watched directory.

> **Q**: Should the shared package rebuild trigger both client and server reloads automatically, or should developers manually restart?
> **A**: Automatic. The `scripts/dev.ts` orchestrator runs `tsc --watch` on shared, and both Vite (client) and `bun --watch` (server) pick up the changed output files. Vite's HMR handles client-side updates. Bun's `--watch` restarts the server when it detects changed files in the shared package's `dist/`. No manual restart needed — the dev experience should be seamless.

---

## 13. Development Workflow

### 13.1 First-Time Setup

- [ ] **PS-DW-001**: Create `scripts/setup.ts` first-time setup script
  - Check that `bun` is installed and meets minimum version
  - Check that `docker` and `docker compose` are available
  - Run `bun install`
  - Copy `.env.example` to `.env` if `.env` doesn't exist
  - Prompt user to fill in required values (or provide defaults)
  - Start Docker services (`docker compose up -d`)
  - Wait for PostgreSQL to be healthy
  - Run database migrations
  - Run database seeds (if any)
  - Print success message with next steps
- [ ] **PS-DW-002**: Add `"setup"` script to root `package.json`
- [ ] **PS-DW-003**: Document the setup process in `README.md`

### 13.2 Daily Development

- [ ] **PS-DW-004**: Document the standard development workflow
  - `docker compose up -d` (start DB if not running)
  - `bun dev` (starts all services)
  - Edit code → HMR updates client, server auto-restarts
  - `bun test` (run tests)
  - `bun lint` (check linting)
  - Commit with conventional commit message
- [ ] **PS-DW-005**: Create `"dev"` script in root `package.json`
  - Verify Docker services are running first
  - Start all dev servers
- [ ] **PS-DW-006**: Create `"test"` script in root `package.json`
  - Run `bun test` across all workspaces
  - Support `--filter` to run tests for a specific workspace
- [ ] **PS-DW-007**: Create `"lint"` script in root `package.json`
  - Run ESLint across all workspaces
  - Run Prettier check across all workspaces
  - Run Stylelint across SCSS files
- [ ] **PS-DW-008**: Create `"format"` script in root `package.json`
  - Run Prettier write across all workspaces
  - Run ESLint fix across all workspaces

### 13.3 Database Workflow

- [ ] **PS-DW-009**: Document database migration workflow
  - How to create a new migration
  - How to run migrations
  - How to rollback migrations
  - How to seed data
- [ ] **PS-DW-010**: Create `"migrate"` script in root `package.json`
- [ ] **PS-DW-011**: Create `"migrate:rollback"` script
- [ ] **PS-DW-012**: Create `"seed"` script

### 13.4 Testing Workflow

- [ ] **PS-DW-013**: Document testing conventions
  - Test file naming: `*.test.ts` or `*.spec.ts`
  - Test file location: colocated with source files
  - Test database setup/teardown
- [ ] **PS-DW-014**: Configure `bun test` at the root
  - Set up test configuration in `bunfig.toml` or package.json
  - Configure coverage reporting
  - Configure test timeout defaults

#### Design Decisions

> **Q**: The PRD specifies `bun test`. Does `bun test` support all needed features (mocking, coverage, watch mode, parallel execution)?
> **A**: Yes. `bun test` supports: built-in mocking (`mock()`), code coverage (`--coverage`), watch mode (`--watch`), snapshot testing, lifecycle hooks (beforeAll, afterEach), and parallel test file execution. It's Jest-compatible in API. The main gap is ecosystem maturity — some niche Jest plugins won't work. For this project's needs (unit tests, integration tests, mocks), `bun test` is sufficient.

> **Q**: Should tests be colocated with source files (`spec.test.ts` next to `spec.ts`), or in a separate `__tests__/` directory?
> **A**: Colocated. Place `spec.service.test.ts` next to `spec.service.ts`. Colocation makes it obvious which files have tests, simplifies imports (relative paths), and encourages writing tests alongside code. Integration tests that span multiple modules go in `server/test/` (or `client/test/`). This is the NestJS convention and works well with `bun test`'s file discovery.

> **Q**: Should there be separate test commands for unit tests, integration tests, and E2E tests, or one unified command with tags/patterns?
> **A**: Separate commands. `bun test` (unit tests, fast), `bun test:integration` (requires DB, slower), `bun test:e2e` (full stack, slowest). Use file naming patterns: `*.test.ts` for unit, `*.integration.test.ts` for integration, `*.e2e.test.ts` for E2E. Bun's `--grep` or file pattern flags separate them. CI runs all three as separate jobs for parallelism and clear failure identification.

> **Q**: Should integration tests use the same Docker PostgreSQL instance (separate database), or spin up a fresh container per test suite?
> **A**: Same Docker instance, separate database. The `docker-compose.test.yml` runs one PostgreSQL container. Each test suite creates a fresh database (or truncates tables) before running. Spinning up a container per suite is slow (~5 seconds per container). A single container with database-per-suite isolation is fast and sufficient. Reset state with `TRUNCATE CASCADE` between suites.

> **Q**: Should there be a test data factory or builder pattern for generating test fixtures?
> **A**: Yes. Create a `test/factories/` directory with builder functions: `createSpec()`, `createEdge()`, `createUser()`, etc. Each factory returns a valid object with sensible defaults and accepts overrides. Use a builder pattern: `SpecFactory.create({ title: 'Custom' })`. This eliminates boilerplate in tests and ensures test data stays consistent with type definitions.

---

## 14. CI/CD Pipeline Configuration

### 14.1 CI Pipeline Definition

- [ ] **PS-CI-001**: Create `.github/workflows/ci.yml` (or equivalent CI config)
  - Trigger on: push to `main`/`develop`, pull requests
  - Define job matrix: lint, test, build, type-check
- [ ] **PS-CI-002**: Define `lint` job
  - Install bun
  - Install dependencies (`bun install`)
  - Run ESLint (`bun lint`)
  - Run Prettier check (`bun format:check`)
- [ ] **PS-CI-003**: Define `type-check` job
  - Install bun and dependencies
  - Run `tsc --noEmit` for each workspace
- [ ] **PS-CI-004**: Define `test` job
  - Install bun and dependencies
  - Start test database (service container or docker compose)
  - Run migrations on test DB
  - Run tests (`bun test`)
  - Upload coverage report
- [ ] **PS-CI-005**: Define `build` job
  - Install bun and dependencies
  - Run production build (`bun run build`)
  - Verify build artifacts exist
  - Optionally analyze bundle size

### 14.2 CI Configuration Files

- [ ] **PS-CI-006**: Create cache configuration for bun dependencies
  - Cache `~/.bun/install/cache` between runs
  - Cache `node_modules` if not using hoisting
- [ ] **PS-CI-007**: Set up environment variables in CI
  - Use CI secrets for sensitive values
  - Use `.env.test` for test-specific values
- [ ] **PS-CI-008**: Configure test database in CI
  - Use PostgreSQL service container
  - Provide connection string via env
- [ ] **PS-CI-009**: Add PR check requirements
  - All CI jobs must pass before merge
  - Code coverage must not decrease

### 14.3 CD Pipeline (Scaffold)

- [ ] **PS-CI-010**: Create `.github/workflows/deploy.yml` (scaffold)
  - Trigger on: push to `main` (or tag)
  - Build Docker images
  - Push to container registry
  - Deploy to staging/production
  - (Details deferred to `11-DEPLOYMENT/PLAN.md`)

#### Design Decisions

> **Q**: Which CI platform will be used? GitHub Actions, GitLab CI, or something else? The plan assumes GitHub Actions but should be confirmed.
> **A**: GitHub Actions. It's free for public repos, has excellent Bun support (`oven-sh/setup-bun` action), native Docker service containers for PostgreSQL, and integrates directly with the GitHub-hosted git remote. The team is likely already using GitHub for code hosting. No reason to introduce an alternative CI platform.

> **Q**: Should CI run on every commit, or only on pull requests and merges to `main`/`develop`?
> **A**: On every pull request and on pushes to `main` and `develop`. Not on every commit to feature branches (that wastes CI minutes on WIP commits). Developers run `bun test` and `bun lint` locally via pre-commit hooks. CI is the safety net for PRs and protected branches. This balances cost with safety.

> **Q**: Should there be a separate CI job per workspace, or a single pipeline that runs everything?
> **A**: Separate jobs per concern (lint, type-check, test-server, test-client, build), not per workspace. Each job installs dependencies once and runs across relevant workspaces. This gives parallel execution (lint and test run simultaneously) with clear failure identification. A single monolithic pipeline is slower and harder to debug.

> **Q**: Should CI cache bun dependencies between runs? What is the cache key strategy (hash of bun.lock)?
> **A**: Yes, cache aggressively. Cache key: hash of `bun.lock`. Restore key: `bun-deps-`. Cache `~/.bun/install/cache` and `node_modules`. GitHub Actions' `actions/cache` handles this. This cuts install time from ~30s to ~5s on cache hit. Also cache TypeScript build info files (`.tsbuildinfo`) for incremental type checking.

> **Q**: Should CI use a Docker-in-Docker approach for the test database, or a CI service container?
> **A**: CI service container. GitHub Actions natively supports `services:` in workflow YAML, which starts a PostgreSQL container alongside the job. This is simpler and faster than Docker-in-Docker (no nested virtualization). Define `postgres:16` as a service with health checks, and pass the connection string via env vars.

> **Q**: What is the target CI execution time? Under 5 minutes? Under 10?
> **A**: Under 5 minutes for the full pipeline (all parallel jobs). Individual jobs should be under 3 minutes. Lint + type-check: ~1 minute. Tests: ~2–3 minutes. Build: ~2 minutes. If CI exceeds 5 minutes, investigate: usually it's dependency install (fix with caching) or slow integration tests (fix with parallelism or faster teardown).

> **Q**: Should `bun audit` (or equivalent) be part of the CI pipeline?
> **A**: Yes. Run `bun audit` (or `npm audit` if Bun's audit is incomplete) as a CI step. Don't block merges on advisories below "high" severity — low/moderate advisories create noise without actionable risk. High and critical advisories should fail CI. This is a simple check that catches known vulnerabilities without manual effort.

> **Q**: Should there be a policy on dependency update frequency (e.g., weekly Dependabot PRs)?
> **A**: Enable GitHub Dependabot with weekly PRs for security updates and monthly PRs for version updates. Group minor/patch updates into a single PR to reduce noise. Major version updates require manual review. This keeps dependencies current without drowning the team in PRs. Review Dependabot PRs in batch during a weekly maintenance window.

> **Q**: Should there be a maximum dependency age before it's flagged for update?
> **A**: No hard maximum. Dependabot's weekly security PRs handle the critical case (known vulnerabilities). For non-security updates, flag dependencies that are 2+ major versions behind in a quarterly review. Chasing every minor update wastes time. The real concern is security — version freshness is secondary.

---

## 15. Editor Configuration

### 15.1 VS Code / Cursor

- [ ] **PS-ED-001**: Create `.vscode/extensions.json` with recommended extensions
  - `dbaeumer.vscode-eslint`
  - `esbenp.prettier-vscode`
  - `bradlc.vscode-tailwindcss` (remove if not using Tailwind)
  - `stylelint.vscode-stylelint` (if using Stylelint)
  - `ms-vscode.vscode-typescript-next`
  - `streetsidesoftware.code-spell-checker`
- [ ] **PS-ED-002**: Create `.vscode/settings.json` (workspace settings)
  - `"editor.formatOnSave"`: `true`
  - `"editor.defaultFormatter"`: `"esbenp.prettier-vscode"`
  - `"editor.codeActionsOnSave"`: `{ "source.fixAll.eslint": "explicit" }`
  - `"typescript.tsdk"`: `"node_modules/typescript/lib"`
  - `"typescript.enablePromptUseWorkspaceTsdk"`: `true`
  - `"files.eol"`: `"\n"`
  - `"search.exclude"`: `{ "**/node_modules": true, "**/dist": true }`
- [ ] **PS-ED-003**: Create `.vscode/launch.json` for debugging
  - Debug configuration for NestJS server (attach to bun)
  - Debug configuration for bun tests
  - Debug configuration for Vite dev server (browser)
- [ ] **PS-ED-004**: Create `.vscode/tasks.json` for common tasks
  - Task: Run dev servers
  - Task: Run tests
  - Task: Run database migration

### 15.2 EditorConfig

- [ ] **PS-ED-005**: Create `.editorconfig` at the repo root
  - `root = true`
  - `[*]`: `indent_style = space`, `indent_size = 2`, `end_of_line = lf`, `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`
  - `[*.md]`: `trim_trailing_whitespace = false`
  - `[Makefile]`: `indent_style = tab`

---

## 16. Documentation Structure

- [ ] **PS-DOC-001**: Create `docs/` directory structure
  - `docs/architecture/` — architecture decision records (ADRs)
  - `docs/api/` — API documentation (auto-generated)
  - `docs/guides/` — developer guides
  - `docs/adr/` — architecture decision records
- [ ] **PS-DOC-002**: Create ADR template at `docs/adr/000-template.md`
  - Title, Status, Context, Decision, Consequences format
- [ ] **PS-DOC-003**: Create first ADR: `docs/adr/001-monorepo-structure.md`
  - Document the decision to use a monorepo with bun workspaces
  - Document alternatives considered (polyrepo, nx, turborepo)
  - Document consequences
- [ ] **PS-DOC-004**: Create `docs/guides/getting-started.md`
  - Prerequisites
  - Clone and setup
  - Running the dev environment
  - Making your first change
- [ ] **PS-DOC-005**: Create `docs/guides/project-conventions.md`
  - File naming conventions
  - Component structure
  - BEM naming rules
  - Import ordering
  - Commit message format

---

## 17. Script Utilities

### 17.1 Development Scripts

- [ ] **PS-SC-001**: Create `scripts/check-deps.ts`
  - Verify bun version meets minimum
  - Verify docker is available
  - Verify git is available
  - Check for required global tools
  - Print status report
- [ ] **PS-SC-002**: Create `scripts/clean.ts`
  - Remove all `node_modules/` directories
  - Remove all `dist/` directories
  - Remove all `*.tsbuildinfo` files
  - Remove coverage reports
  - Optionally remove docker volumes (`--deep` flag)
- [ ] **PS-SC-003**: Create `scripts/reset-db.ts`
  - Drop and recreate the development database
  - Run all migrations
  - Run seeds
  - Print confirmation

### 17.2 Generator Scripts

- [ ] **PS-SC-004**: Create `scripts/generate-module.ts`
  - Generate a new NestJS module with boilerplate
  - Input: module name
  - Output: module, controller, service, dto, test files
  - Respects project conventions (BEM, ESM, etc.)
- [ ] **PS-SC-005**: Create `scripts/generate-component.ts`
  - Generate a new React component with boilerplate
  - Input: component name
  - Output: ComponentName.tsx, ComponentName.scss, ComponentName.test.tsx
  - Respects BEM naming convention
- [ ] **PS-SC-006**: Create `scripts/generate-gen-ui.ts`
  - Scaffold a new generative UI project in `client/gen/`
  - Input: user name, project name
  - Output: minimal Vite project with package.json, vite.config.ts, index.html, main.tsx

### 17.3 Validation Scripts

- [ ] **PS-SC-007**: Create `scripts/validate-workspace.ts`
  - Check that all workspace package.json files are valid
  - Check that all workspace dependencies resolve
  - Check that shared types compile
  - Check that no circular dependencies exist between workspaces
- [ ] **PS-SC-008**: Create `scripts/validate-env.ts`
  - Compare `.env` against `.env.example`
  - Report missing variables
  - Report extra variables not in the template

---

## Summary

### Task Count by Section

| Section | Tasks |
|---------|-------|
| 1. Monorepo Root Layout | 98 (PS-RL-001 through PS-RL-098) |
| 2. Bun Workspace Configuration | 12 (PS-WS-001 through PS-WS-012) |
| 3. Shared Types Package | 40 (PS-ST-001 through PS-ST-040) |
| 4. TypeScript Configuration | 10 (PS-TS-001 through PS-TS-010) |
| 5. Vite Configuration | 9 (PS-VT-001 through PS-VT-009) |
| 6. ESLint Configuration | 8 (PS-EL-001 through PS-EL-008) |
| 7. Prettier Configuration | 5 (PS-PR-001 through PS-PR-005) |
| 8. SCSS / Styling Configuration | 11 (PS-SC-001 through PS-SC-011) |
| 9. Environment Configuration | 9 (PS-ENV-001 through PS-ENV-009) |
| 10. Git Configuration | 9 (PS-GIT-001 through PS-GIT-009) |
| 11. Docker Setup | 7 (PS-DK-001 through PS-DK-007) |
| 12. Build System | 10 (PS-BD-001 through PS-BD-010) |
| 13. Development Workflow | 14 (PS-DW-001 through PS-DW-014) |
| 14. CI/CD Pipeline | 10 (PS-CI-001 through PS-CI-010) |
| 15. Editor Configuration | 5 (PS-ED-001 through PS-ED-005) |
| 16. Documentation Structure | 5 (PS-DOC-001 through PS-DOC-005) |
| 17. Script Utilities | 8 (PS-SC-001 through PS-SC-008) |
| **TOTAL** | **270** |

### Dependencies (What This Plan Enables)

Completion of this plan unblocks:
- `02-FRONTEND/01-ARCHITECTURE-PLAN.md` — needs client workspace, Vite config, SCSS setup
- `03-SERVER/01-ARCHITECTURE-PLAN.md` — needs server workspace, NestJS skeleton
- `04-KNOWLEDGE-GRAPH/01-ARCHITECTURE-PLAN.md` — needs knowledge-graph directory structure
- `07-DATABASE/PLAN.md` — needs Docker PostgreSQL, server workspace
- `08-TESTING/01-STRATEGY-PLAN.md` — needs test harness, CI pipeline
- `11-DEPLOYMENT/PLAN.md` — needs Docker scaffolding

### Definition of Done

This plan is complete when:
- [ ] `bun install` succeeds from a fresh clone
- [ ] `bun dev` starts client, server, and database
- [ ] `bun build` produces production artifacts for client and server
- [ ] `bun test` runs (even if no tests exist yet)
- [ ] `bun lint` runs with no errors on the scaffold
- [ ] TypeScript compilation succeeds across all workspaces
- [ ] `@kg/shared` types are importable from both `@kg/client` and `@kg/server`
- [ ] Git hooks fire on commit and push
- [ ] CI pipeline runs all checks on a PR
- [ ] Docker compose starts PostgreSQL and the server can connect

---

## Additional Design Decisions

### Versioning & Releases

> **Q**: Should workspace packages use independent versioning, or should all packages share a single version number?
> **A**: Single version number across all packages. Independent versioning adds complexity to a monorepo where all packages are deployed together. Bump the version in the root `package.json` and let workspace packages reference it (or use `workspace:*` for inter-package deps). Semantic versioning applies to the product as a whole, not individual packages.

> **Q**: Should there be an automated release process (e.g., semantic-release), or manual version bumps?
> **A**: Manual version bumps for now. Semantic-release adds infrastructure complexity before there's a release cadence. Use `npm version` (or a script) to bump the version, create a git tag, and update changelogs. Automate in Phase 5 if release frequency warrants it. The priority is building features, not release tooling.

> **Q**: Should the API be versioned from the start (`/api/v1/`), or is that premature? If versioned, what is the deprecation policy?
> **A**: Version from the start: all endpoints under `/api/v1/`. It costs nothing upfront and avoids a painful migration later. The `v1` prefix is already in the master plan's API routes. Deprecation policy: when `v2` is introduced, `v1` receives 6 months of maintenance (bug fixes only). For an internal tool, this is generous — in practice, migrate quickly and drop `v1`.

### Code Style & Conventions

> **Q**: Should imports use file extensions (`.js` for ESM compatibility), or rely on Bun/Vite resolution without extensions?
> **A**: No file extensions. Bun and Vite both resolve `.ts`/`.tsx` imports without extensions. Adding `.js` extensions to TypeScript imports is confusing and unnecessary when the runtime handles resolution. The `tsconfig.json` sets `moduleResolution: "bundler"`, which explicitly supports extensionless imports. If strict ESM compliance is ever needed, a codemod can add extensions.

> **Q**: Should barrel exports (`index.ts`) be used everywhere, or only at package boundaries? Barrel exports can cause circular dependency issues and larger bundle sizes.
> **A**: Only at package boundaries and major directories. `@kg/shared` gets a barrel export (`src/index.ts`). Major feature directories in the server (`modules/specs/index.ts`) get barrels. Individual component directories do NOT get barrels — import directly from the component file. This prevents circular dependency chains and keeps Vite's tree-shaking effective. Barrel depth: max 1 level.

> **Q**: Should React component files use PascalCase (`SpecEditor.tsx`) and non-component files use camelCase (`apiClient.ts`)?
> **A**: Yes. React components: PascalCase (`SpecEditor.tsx`, `ChatDialog.tsx`). Non-component files: camelCase (`apiClient.ts`, `useSpecEditor.ts`, `graphUtils.ts`). SCSS files: match their component's PascalCase (`SpecEditor.scss`). This is the React community convention and makes it instantly clear which files export React components.

> **Q**: Should NestJS files follow the NestJS convention (`spec.controller.ts`, `spec.service.ts`, `spec.module.ts`)?
> **A**: Yes. Follow NestJS naming conventions exactly: `spec.controller.ts`, `spec.service.ts`, `spec.module.ts`, `spec.dto.ts`, `spec.guard.ts`. This is expected by NestJS CLI generators, documentation, and the community. It makes server code navigable by anyone familiar with NestJS. The kebab-case + suffix pattern is specific to the server package.

> **Q**: Should test files use `.test.ts` or `.spec.ts`? NestJS convention is `.spec.ts`, but bun test defaults to `.test.ts`.
> **A**: Use `.test.ts` everywhere. `bun test` discovers `.test.ts` files by default, and the project uses `bun test` as the runner. Consistency across client and server is more valuable than following NestJS convention on this one point. Configure NestJS generators (if used) to output `.test.ts` instead of `.spec.ts`. The term "spec" is also overloaded in this project (knowledge specs).

> **Q**: Should directories use kebab-case (`knowledge-graph/`), camelCase (`knowledgeGraph/`), or match the PascalCase BEM convention?
> **A**: kebab-case for all directories: `knowledge-graph/`, `spec-editor/`, `chat-dialog/`. This is the filesystem convention across the JavaScript ecosystem, works on case-sensitive and case-insensitive filesystems, and matches the plan directory naming (`01-PROJECT-STRUCTURE/`). PascalCase BEM applies to CSS class names only, not filesystem paths.

> **Q**: Should the directory structure be feature-based (all auth files together) or layer-based (all controllers together, all services together)? NestJS convention is feature-based.
> **A**: Feature-based on the server (NestJS convention): `modules/auth/` contains `auth.controller.ts`, `auth.service.ts`, `auth.module.ts`, `auth.dto.ts`, `auth.guard.ts`. Feature-based on the client: `components/SpecEditor/` contains `SpecEditor.tsx`, `SpecEditor.scss`, `SpecEditor.test.tsx`. Layer-based creates cross-directory dependencies that are hard to navigate. Feature-based keeps related code together.
