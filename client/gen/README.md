# Generated UI Components

This directory holds dynamically generated UI component projects created by the GenUI agent.

Each generated component gets its own subdirectory with:

- A `package.json` with dependencies from the approved allowlist
- Source files (React + TypeScript)
- Built output ready for iframe embedding

These projects are sandboxed and isolated from the main application.
